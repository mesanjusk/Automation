# Fail installs on unsupported Node versions instead of producing a broken
# tree (package.json "engines" is advisory without this flag).
engine-strict=true

# Keep installs deterministic across hosts: never write a different lockfile
# shape because of a newer npm on the build machine.
lockfile-version=3
